    <%if (filterYesNo) { %>
    /**
     * For e.g. http://localhost:8080/<%= nounLowercasePlural %>?filter=...
     */
    @RequestMapping(value = "/<%= nounLowercasePlural %>", params = {"filter"}, method = RequestMethod.GET)
    public ResponseEntity<List<<%= noun %>>> findFiltered(@RequestParam("filter") String filter) {
        List<<%= noun %>> filteredList = <%= nounLowercase %>Service.findAll()
                .stream().filter(p -> p.get<%= filterNameUpper %>().equals(filter)).collect(Collectors.toCollection(ArrayList::new));
        return new ResponseEntity<>(filteredList, HttpStatus.OK);
    }
    <% } %>